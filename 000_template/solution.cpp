// include the standard library (feature of g++)
#include <bits/stdc++.h>

using namespace std;

int main() {
    // use fast io
    ios::sync_with_stdio(0);
    cin.tie(0);

    int a, b;
    string x;
    cin >> a >> b >> x;

    long long n;
    cin >> n;

    while (true) {
        // Do something
        cout << n << " ";
    }

    for (int i = 0; i < n - 1; i++) {
        long long num;
        cin >> num;
        // Do something
    }

    cout << n < "\n";
    cout << a << " " << b << " " << x << "\n";

    double x = 0.3*3+0.1;
    printf("%.20f\n", x); // 0.99999999999999988898

}